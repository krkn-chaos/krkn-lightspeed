# krknctl Lightspeed FAISS-only RAG (no LLM)
# Minimal dependencies for vector-based scenario identification

# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Vector store and embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0,<3.0.0

# LangChain minimal (for Document compatibility only)
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1
typing-extensions>=4.8.0

# Utilities
tqdm>=4.66.0
huggingface-hub>=0.16.0

# Document processing
unstructured[md]>=0.10.0